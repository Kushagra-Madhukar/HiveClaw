[package]
name = "aria-learning"
version = "0.1.0"
edition = "2021"
description = "ARIA-X learning-plane types and task fingerprinting"

[dependencies]
aria-core = { path = "../aria-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
